MASSIVE_API_KEY=replace_with_your_server_side_api_key
# Optional: raises StockTwits rate limits for external crowd sentiment.
# Without it the public symbol stream is used best-effort with D1 caching.
STOCKTWITS_TOKEN=
